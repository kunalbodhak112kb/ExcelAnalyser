<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin Panel</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
        }

        .container {
            display: flex;
            height: 100vh;
        }

        .sidebar {
            width: 220px;
            background-color: #f2f2f2;
            padding-top: 20px;
        }

        .sidebar a {
            display: block;
            padding: 12px 16px;
            text-decoration: none;
            color: #333;
            border-bottom: 1px solid #ccc;
        }

        .sidebar a:hover {
            background-color: #ddd;
        }

        .main-content {
            flex-grow: 1;
            padding: 20px;
        }

        .topbar {
            display: flex;
            justify-content: flex-end;
        }

        .logout-btn {
            padding: 6px 12px;
            background-color: crimson;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        h2 {
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="sidebar">
            <a href="/dashboard">Dashboard</a>
            <a href="/admin/history">Users History</a>
            <a href="/admin/summary">File Summary</a>
            <a href="/admin/modify">Modify Users</a>
        </div>
        <div class="main-content">
            <div class="topbar">
                <form action="/logout" method="POST">
                    <button type="submit" class="logout-btn">Logout</button>
                </form>
            </div>
            <h2>Welcome, Admin</h2>
            <p>This is your admin control panel.</p>
        </div>
    </div>
</body>
</html>
